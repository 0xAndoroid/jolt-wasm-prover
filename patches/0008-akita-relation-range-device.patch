diff --git a/crates/akita-prover/src/lib.rs b/crates/akita-prover/src/lib.rs
index be429f5..83ae10a 100644
--- a/crates/akita-prover/src/lib.rs
+++ b/crates/akita-prover/src/lib.rs
@@ -78,6 +78,12 @@ pub use protocol::sumcheck::digit_range::device::{
     DigitRangeSession, FIELD_BYTES as DIGIT_RANGE_FIELD_BYTES,
     MESSAGE_BYTES as DIGIT_RANGE_MESSAGE_BYTES,
 };
+pub use protocol::sumcheck::relation_range_image::device::{
+    set_relation_range_device, set_relation_range_parity_rounds, RelationRangeDevice,
+    RelationRangeJob, RelationRangeSession, RelationRangeShape,
+    FIELD_BYTES as RELATION_RANGE_FIELD_BYTES, MESSAGE_BYTES as RELATION_RANGE_MESSAGE_BYTES,
+    TERMS as RELATION_RANGE_TERMS,
+};
 pub use protocol::sumcheck::{
     DigitRangeProver, LowBasisRangeCheckProver, RelationRangeImageProver,
 };
diff --git a/crates/akita-prover/src/protocol/core/fold/stages.rs b/crates/akita-prover/src/protocol/core/fold/stages.rs
index 7473241..1ce5807 100644
--- a/crates/akita-prover/src/protocol/core/fold/stages.rs
+++ b/crates/akita-prover/src/protocol/core/fold/stages.rs
@@ -223,6 +223,7 @@ where
                 .ok_or_else(|| AkitaError::InvalidSetup("Stage 2 round overflow".into()))?;
             Ok(challenge)
         })?;
+    stage2_prover.take_device_error()?;
     if final_claim != stage2_prover.expected_final_claim()? {
         return Err(AkitaError::InvalidInput(
             "stage-2 prover final claim disagrees with its folded oracle".into(),
diff --git a/crates/akita-prover/src/protocol/sumcheck/relation_range_image/device.rs b/crates/akita-prover/src/protocol/sumcheck/relation_range_image/device.rs
new file mode 100644
index 0000000..10e6d1e
--- /dev/null
+++ b/crates/akita-prover/src/protocol/sumcheck/relation_range_image/device.rs
@@ -0,0 +1,154 @@
+//! Optional device seam for the reduced-dense stage-2 sumcheck rounds.
+//!
+//! A [`RelationRangeDevice`] may take the first rounds of one
+//! `ReducedDense` instance: it receives the compact digit witness and the
+//! flat relation weights (dense weights plus the structured linear terms,
+//! one field element per padded address) once, then per round the previous
+//! challenge and the prover's remaining Gruen eq tables, and returns the six
+//! round terms (norm `q0,q1,q2`, relation `c0,c1,c2`). After its last round
+//! it hands the folded witness and weight tables back and the CPU prover
+//! finishes with the weights in dense form, so the transcript, challenge
+//! derivation and proof layout are unchanged.
+
+use std::sync::atomic::{AtomicUsize, Ordering};
+use std::sync::{Arc, OnceLock};
+
+use akita_error::AkitaError;
+use akita_serialization::{AkitaSerialize, Compress};
+use jolt_field::solinas::parallel::*;
+use jolt_field::{Field, Ring};
+
+/// Canonical little-endian byte length of one field element on the wire.
+pub const FIELD_BYTES: usize = 16;
+/// Round messages carry `virt[q0, q1, q2]` then `rel[c0, c1, c2]`.
+pub const TERMS: usize = 6;
+pub const MESSAGE_BYTES: usize = TERMS * FIELD_BYTES;
+
+/// Shape of one instance, offered before the weights are materialized.
+pub struct RelationRangeShape {
+    /// Live witness length `live_lane_count * coeff_count`.
+    pub len: usize,
+    /// Padded relation domain (power of two, `>= len`).
+    pub domain_len: usize,
+    pub coefficient_bits: usize,
+    pub lane_bits: usize,
+    pub bit_width: u8,
+}
+
+/// One reduced-dense instance as bytes.
+pub struct RelationRangeJob<'a> {
+    pub shape: RelationRangeShape,
+    /// `len` digits in two's complement at `bit_width` bits each, LSB-first
+    /// contiguous (`PackedSignedDigits` encoding), flat index
+    /// `lane * coeff_count + coefficient`.
+    pub digits: &'a [u8],
+    /// `domain_len` weights, 16 B LE each: dense relation weight plus the
+    /// structured linear term at that flat address (zero beyond `len`).
+    pub weights: &'a [u8],
+}
+
+pub trait RelationRangeSession: Send + Sync {
+    /// Fold the witness and weight tables by `prev` (absent in round 0),
+    /// then write round `round`'s six terms (16 B LE each) into `out`.
+    /// `e_first`/`e_second` are this round's remaining Gruen tables.
+    fn round(
+        &mut self,
+        round: usize,
+        prev: Option<&[u8; FIELD_BYTES]>,
+        e_first: &[u8],
+        e_second: &[u8],
+        out: &mut [u8; MESSAGE_BYTES],
+    ) -> Result<(), AkitaError>;
+
+    /// `(witness, weights)` as the last round evaluated them (folded by
+    /// every earlier challenge), 16 B LE per entry; the witness has
+    /// `ceil(len / 2^rounds_folded)` entries, the weights `domain_len >>
+    /// rounds_folded`.
+    fn tables(self: Box<Self>) -> Result<(Vec<u8>, Vec<u8>), AkitaError>;
+}
+
+pub trait RelationRangeDevice: Send + Sync {
+    /// Rounds the device would run on this shape; 0 declines (nothing is
+    /// materialized for declined instances).
+    fn rounds_for(&self, shape: &RelationRangeShape) -> usize;
+
+    /// Open a session for `rounds` rounds (as returned by `rounds_for`).
+    fn open(
+        &self,
+        job: &RelationRangeJob<'_>,
+        rounds: usize,
+    ) -> Result<Box<dyn RelationRangeSession>, AkitaError>;
+}
+
+static DEVICE: OnceLock<Arc<dyn RelationRangeDevice>> = OnceLock::new();
+static PARITY_ROUNDS: AtomicUsize = AtomicUsize::new(0);
+
+/// Install the process-wide device; fails if one is already installed.
+pub fn set_relation_range_device(device: Arc<dyn RelationRangeDevice>) -> Result<(), AkitaError> {
+    DEVICE
+        .set(device)
+        .map_err(|_| AkitaError::InvalidSetup("relation-range device already installed".into()))
+}
+
+/// Keep the CPU prover authoritative and check the device's first `rounds`
+/// messages and its handoff tables against it; a mismatch fails the prove.
+pub fn set_relation_range_parity_rounds(rounds: usize) {
+    PARITY_ROUNDS.store(rounds, Ordering::SeqCst);
+}
+
+pub(super) fn installed_device() -> Option<&'static Arc<dyn RelationRangeDevice>> {
+    DEVICE.get()
+}
+
+pub(super) fn parity_rounds() -> usize {
+    PARITY_ROUNDS.load(Ordering::SeqCst)
+}
+
+pub(super) fn field_to_bytes<E: Field + AkitaSerialize>(value: &E, out: &mut [u8]) {
+    let mut cursor = std::io::Cursor::new(out);
+    value
+        .serialize_with_mode(&mut cursor, Compress::No)
+        .expect("16-byte field element serializes into a 16-byte slot");
+}
+
+pub(super) fn fields_to_bytes<E: Field + AkitaSerialize>(values: &[E]) -> Vec<u8> {
+    let mut out = vec![0u8; values.len() * FIELD_BYTES];
+    cfg_chunks_mut!(out, FIELD_BYTES * 4096)
+        .zip(cfg_chunks!(values, 4096))
+        .for_each(|(chunk, values)| {
+            for (value, slot) in values.iter().zip(chunk.chunks_exact_mut(FIELD_BYTES)) {
+                field_to_bytes(value, slot);
+            }
+        });
+    out
+}
+
+pub(super) fn field_from_bytes<E: Ring>(bytes: &[u8]) -> E {
+    let mut word = [0u8; FIELD_BYTES];
+    word.copy_from_slice(bytes);
+    E::from_u128(u128::from_le_bytes(word))
+}
+
+pub(super) fn fields_from_bytes<E: Ring>(bytes: &[u8]) -> Result<Vec<E>, AkitaError> {
+    if !bytes.len().is_multiple_of(FIELD_BYTES) {
+        return Err(AkitaError::InvalidSize {
+            expected: bytes.len().div_ceil(FIELD_BYTES) * FIELD_BYTES,
+            actual: bytes.len(),
+        });
+    }
+    Ok(bytes
+        .chunks_exact(FIELD_BYTES)
+        .map(field_from_bytes)
+        .collect())
+}
+
+/// Device state of one prover while its rounds run on the device.
+pub(super) struct DeviceRounds<E: Field> {
+    pub(super) session: Option<Box<dyn RelationRangeSession>>,
+    pub(super) rounds: usize,
+    pub(super) prev: Option<E>,
+    /// CPU state stays authoritative; device terms and tables are compared.
+    pub(super) parity: bool,
+    pub(super) witness_len: usize,
+    pub(super) domain_len: usize,
+}
diff --git a/crates/akita-prover/src/protocol/sumcheck/relation_range_image/device_flow.rs b/crates/akita-prover/src/protocol/sumcheck/relation_range_image/device_flow.rs
new file mode 100644
index 0000000..996105f
--- /dev/null
+++ b/crates/akita-prover/src/protocol/sumcheck/relation_range_image/device_flow.rs
@@ -0,0 +1,251 @@
+use super::device::{
+    field_from_bytes, field_to_bytes, fields_from_bytes, fields_to_bytes, installed_device,
+    parity_rounds, DeviceRounds, RelationRangeJob, RelationRangeShape, FIELD_BYTES, MESSAGE_BYTES,
+    TERMS,
+};
+use super::*;
+use akita_serialization::{AkitaSerialize, Compress};
+
+/// What `ingest_reduced_dense_challenge` does with the CPU state after the
+/// device was consulted.
+pub(super) enum DeviceFold<E: Field> {
+    /// The device owns the tables this round: skip the CPU fold.
+    Skip,
+    /// Fold on the CPU; with `Some`, compare the handed-back tables (already
+    /// folded by this challenge) against the CPU state afterwards.
+    Cpu(Option<(Vec<E>, Vec<E>)>),
+}
+
+impl<E: Field + Ring + Unreduced + AkitaSerialize> RelationRangeImageProver<E> {
+    /// Offer a `ReducedDense` instance to the installed device.
+    pub(super) fn try_open_device(&mut self) {
+        let Some(device) = installed_device() else {
+            return;
+        };
+        if self.additional_relation_terms.is_some()
+            || E::zero().serialized_size(Compress::No) != FIELD_BYTES
+            || self.num_vars < 2
+        {
+            return;
+        }
+        let (
+            RelationRoundState::ReducedDense { weights: dense },
+            WitnessState::CompactPrefix(compact),
+        ) = (&self.relation_state, &self.witness_state)
+        else {
+            return;
+        };
+        let shape = RelationRangeShape {
+            len: compact.len(),
+            domain_len: dense.evaluations().len(),
+            coefficient_bits: self.coefficient_bits(),
+            lane_bits: self.lane_bits,
+            bit_width: compact.bit_width(),
+        };
+        let rounds = device.rounds_for(&shape).min(self.num_vars - 1);
+        if rounds == 0 {
+            return;
+        }
+        let started = Instant::now();
+        let mut flat = dense.evaluations().to_vec();
+        self.linear_terms.add_flat_into(&mut flat);
+        let weights = fields_to_bytes(&flat);
+        drop(flat);
+        let weights_ms = started.elapsed().as_secs_f64() * 1e3;
+        let job = RelationRangeJob {
+            shape,
+            digits: compact.encoded_bytes(),
+            weights: &weights,
+        };
+        match device.open(&job, rounds) {
+            Ok(session) => {
+                let parity = parity_rounds() > 0;
+                tracing::info!(rounds, parity, weights_ms, "stage2_device_plan");
+                self.device = Some(DeviceRounds {
+                    session: Some(session),
+                    rounds,
+                    prev: None,
+                    parity,
+                    witness_len: job.shape.len,
+                    domain_len: job.shape.domain_len,
+                });
+            }
+            Err(e) => self.device_error = Some(e),
+        }
+    }
+
+    /// The device's terms for the current round (`None` when the device is
+    /// not running this round). A device error is recorded and surfaces
+    /// through `take_device_error`; the returned terms are then zero.
+    pub(super) fn device_round_terms(&mut self) -> Option<(NormRoundTerms<E>, [E; 3])> {
+        let round = self.rounds_completed;
+        let can_skip = self.can_skip_norm_linear_coeff();
+        let (e_first, e_second) = self.split_eq.remaining_eq_tables();
+        let e_first = fields_to_bytes(e_first);
+        let e_second = fields_to_bytes(e_second);
+        let d = self.device.as_mut()?;
+        if round >= d.rounds {
+            return None;
+        }
+        let terms = (|| -> Result<[E; TERMS], AkitaError> {
+            let session = d
+                .session
+                .as_mut()
+                .ok_or_else(|| AkitaError::InvalidSetup("relation-range session closed".into()))?;
+            let prev = d.prev.map(|r| {
+                let mut bytes = [0u8; FIELD_BYTES];
+                field_to_bytes(&r, &mut bytes);
+                bytes
+            });
+            let mut out = [0u8; MESSAGE_BYTES];
+            session.round(round, prev.as_ref(), &e_first, &e_second, &mut out)?;
+            let mut terms = [E::zero(); TERMS];
+            for (term, bytes) in terms.iter_mut().zip(out.chunks_exact(FIELD_BYTES)) {
+                *term = field_from_bytes(bytes);
+            }
+            Ok(terms)
+        })();
+        let terms = match terms {
+            Ok(terms) => terms,
+            Err(e) => {
+                self.device_error.get_or_insert(e);
+                [E::zero(); TERMS]
+            }
+        };
+        let virt = if can_skip {
+            NormRoundTerms::SkipLinear([terms[0], terms[2]])
+        } else {
+            NormRoundTerms::Full([terms[0], terms[1], terms[2]])
+        };
+        Some((virt, [terms[3], terms[4], terms[5]]))
+    }
+}
+
+impl<E: Field + Ring + Unreduced + Fold> RelationRangeImageProver<E> {
+    /// The first device error, if any: trait methods cannot fail, so the seam
+    /// records the error, keeps the rounds flowing and surfaces it here.
+    pub fn take_device_error(&mut self) -> Result<(), AkitaError> {
+        match self.device_error.take() {
+            Some(e) => Err(e),
+            None => Ok(()),
+        }
+    }
+
+    /// Record challenge `r` for the device; on its last round take the
+    /// tables back (folded by `r` here) and, unless parity mode keeps the
+    /// CPU authoritative, resume the CPU prover from them.
+    pub(super) fn device_ingest(&mut self, r: E) -> DeviceFold<E> {
+        let Some(d) = self.device.as_mut() else {
+            return DeviceFold::Cpu(None);
+        };
+        let round = self.rounds_completed;
+        if round + 1 < d.rounds {
+            d.prev = Some(r);
+            return if d.parity {
+                DeviceFold::Cpu(None)
+            } else {
+                DeviceFold::Skip
+            };
+        }
+        let parity = d.parity;
+        let (witness_len, domain_len) = (d.witness_len, d.domain_len);
+        let tables = d
+            .session
+            .take()
+            .ok_or_else(|| AkitaError::InvalidSetup("relation-range session closed".into()))
+            .and_then(|session| session.tables())
+            .and_then(|(w, p)| Ok((fields_from_bytes::<E>(&w)?, fields_from_bytes::<E>(&p)?)));
+        self.device = None;
+        let (mut witness, mut weights) = match tables {
+            Ok(tables) => tables,
+            Err(e) => {
+                self.device_error.get_or_insert(e);
+                return DeviceFold::Cpu(None);
+            }
+        };
+        let rounds = round + 1;
+        let expected_witness = witness_len.div_ceil(1 << (rounds - 1));
+        let expected_weights = domain_len >> (rounds - 1);
+        if witness.len() != expected_witness || weights.len() != expected_weights {
+            self.device_error.get_or_insert(AkitaError::InvalidSize {
+                expected: expected_witness + expected_weights,
+                actual: witness.len() + weights.len(),
+            });
+            return DeviceFold::Cpu(None);
+        }
+        fold_evals_in_place(&mut witness, r);
+        fold_evals_in_place(&mut weights, r);
+        if parity {
+            return DeviceFold::Cpu(Some((witness, weights)));
+        }
+        let live_len = witness_len.div_ceil(1 << rounds);
+        match DenseRelationWeights::new(weights, live_len) {
+            Ok(dense) => self.relation_state = RelationRoundState::ReducedDense { weights: dense },
+            Err(e) => {
+                self.device_error.get_or_insert(e);
+                return DeviceFold::Cpu(None);
+            }
+        }
+        self.witness_state = WitnessState::FoldedSuffix(witness);
+        let coeff_count = 1usize << self.coefficient_bits().saturating_sub(rounds);
+        let live_lanes = if rounds > self.coefficient_bits() {
+            self.live_lane_count.div_ceil(2)
+        } else {
+            self.live_lane_count
+        };
+        self.linear_terms = PreparedProverLinearTerms::zero(live_lanes, coeff_count);
+        DeviceFold::Skip
+    }
+
+    /// Parity mode: the handed-back tables must equal the CPU state.
+    pub(super) fn check_device_tables(&mut self, witness: &[E], weights: &[E]) {
+        let cpu_witness = match &self.witness_state {
+            WitnessState::FoldedSuffix(w) => w.as_slice(),
+            WitnessState::CompactPrefix(_) => &[],
+        };
+        let mut cpu_weights = match &self.relation_state {
+            RelationRoundState::ReducedDense { weights } => weights.evaluations().to_vec(),
+            RelationRoundState::QuotientFactored { .. } => Vec::new(),
+        };
+        self.linear_terms.add_flat_into(&mut cpu_weights);
+        if cpu_witness != witness || cpu_weights != weights {
+            self.device_error.get_or_insert(AkitaError::InvalidSetup(format!(
+                "relation-range device handoff mismatch after round {} (witness {} vs {}, weights {} vs {})",
+                self.rounds_completed,
+                witness.len(),
+                cpu_witness.len(),
+                weights.len(),
+                cpu_weights.len()
+            )));
+        } else {
+            tracing::info!(round = self.rounds_completed, "stage2_device_tables_ok");
+        }
+    }
+}
+
+impl<E: Field + Ring + Unreduced> RelationRangeImageProver<E> {
+    /// Parity mode: the device's round terms must equal the CPU's.
+    pub(super) fn check_device_terms(
+        &mut self,
+        cpu: &(NormRoundTerms<E>, [E; 3]),
+        dev: &(NormRoundTerms<E>, [E; 3]),
+    ) {
+        if self.rounds_completed >= parity_rounds() {
+            return;
+        }
+        let same_virt = match (cpu.0, dev.0) {
+            (NormRoundTerms::Full(a), NormRoundTerms::Full(b)) => a == b,
+            (NormRoundTerms::SkipLinear(a), NormRoundTerms::SkipLinear(b)) => a == b,
+            _ => false,
+        };
+        if same_virt && cpu.1 == dev.1 {
+            tracing::info!(round = self.rounds_completed, "stage2_device_parity_ok");
+        } else {
+            self.device_error
+                .get_or_insert(AkitaError::InvalidSetup(format!(
+                    "relation-range device parity mismatch at round {} (num_vars {})",
+                    self.rounds_completed, self.num_vars
+                )));
+        }
+    }
+}
diff --git a/crates/akita-prover/src/protocol/sumcheck/relation_range_image/evaluation_trace.rs b/crates/akita-prover/src/protocol/sumcheck/relation_range_image/evaluation_trace.rs
index 941df54..f141f64 100644
--- a/crates/akita-prover/src/protocol/sumcheck/relation_range_image/evaluation_trace.rs
+++ b/crates/akita-prover/src/protocol/sumcheck/relation_range_image/evaluation_trace.rs
@@ -13,6 +13,7 @@ use akita_types::{
     CoefficientPackingStage2Source, CoefficientPackingStage2Terms, EvaluationTraceInputs,
     FpExtEncoding,
 };
+use jolt_field::solinas::parallel::*;
 use jolt_field::{CanonicalEncoding, ExtField, Field, Ring};
 
 /// One contiguous physical opening-digit run for a claim inside one witness chunk.
@@ -232,6 +233,33 @@ impl<E: Field> PreparedProverLinearTerms<E> {
         self.sources.len()
     }
 
+    /// Add the structured linear term of every live flat address
+    /// (`lane * coeff_count + coefficient`) into `out`.
+    pub(crate) fn add_flat_into(&self, out: &mut [E]) {
+        let coeff_count = self.coeff_count;
+        let live = &mut out[..self.live_lane_count * coeff_count];
+        cfg_chunks_mut!(live, coeff_count)
+            .enumerate()
+            .for_each(|(lane, row)| {
+                self.resolve_lane(lane).for_each_factored(|factor, values| {
+                    for (slot, value) in row.iter_mut().zip(values) {
+                        *slot += factor * *value;
+                    }
+                });
+            });
+    }
+
+    /// `(lane-weight kind, sources, source values)` for the stage-2 plan log line.
+    pub(crate) fn describe(&self) -> (&'static str, usize, usize) {
+        let kind = match &self.lane_weights {
+            PreparedLaneWeights::Sparse(_) => "sparse",
+            PreparedLaneWeights::Packing(_) => "packing",
+            PreparedLaneWeights::Dense(_) => "dense",
+        };
+        let values = self.sources.iter().map(|s| s.values.len()).sum();
+        (kind, self.sources.len(), values)
+    }
+
     pub(crate) fn final_value(&self) -> Result<E, AkitaError> {
         if self.live_lane_count != 1 || self.coeff_count != 1 {
             return Err(AkitaError::InvalidProof);
diff --git a/crates/akita-prover/src/protocol/sumcheck/relation_range_image/lifecycle.rs b/crates/akita-prover/src/protocol/sumcheck/relation_range_image/lifecycle.rs
index 799d302..92180b8 100644
--- a/crates/akita-prover/src/protocol/sumcheck/relation_range_image/lifecycle.rs
+++ b/crates/akita-prover/src/protocol/sumcheck/relation_range_image/lifecycle.rs
@@ -1,4 +1,5 @@
 use super::*;
+use akita_serialization::AkitaSerialize;
 
 fn stage2_geometry(
     lane_bits: usize,
@@ -17,7 +18,7 @@ fn stage2_geometry(
     Ok((lane_capacity, coeff_count))
 }
 
-impl<E: Field + Ring + Unreduced> RelationRangeImageProver<E> {
+impl<E: Field + Ring + Unreduced + AkitaSerialize> RelationRangeImageProver<E> {
     /// Create a stage-2 instance containing only the virtual range-image term.
     ///
     /// This is the standalone companion to
@@ -179,6 +180,23 @@ impl<E: Field + Ring + Unreduced> RelationRangeImageProver<E> {
             }
         }
 
+        let (linear_kind, linear_sources, linear_values) = linear_terms.describe();
+        tracing::info!(
+            mode = match &relation_weights {
+                RelationWeightOracle::QuotientFactored(_) => "quotient_factored",
+                RelationWeightOracle::ReducedDense(_) => "reduced_dense",
+            },
+            coefficient_bits,
+            lane_bits,
+            live_lane_count,
+            witness_len,
+            b,
+            linear_kind,
+            linear_sources,
+            linear_values,
+            additional = additional_relation_terms.is_some(),
+            "stage2_plan"
+        );
         let relation_linear_claim = relation_claim + linear_opening_claim;
         let additional_claim = additional_relation_terms
             .as_ref()
@@ -231,7 +249,7 @@ impl<E: Field + Ring + Unreduced> RelationRangeImageProver<E> {
             }
         };
 
-        Ok(Self {
+        let mut prover = Self {
             witness_state: WitnessState::CompactPrefix(w_evals_compact),
             b,
             input_claim,
@@ -248,9 +266,15 @@ impl<E: Field + Ring + Unreduced> RelationRangeImageProver<E> {
             scan_time_total: 0.0,
             fold_time_total: 0.0,
             rounds_completed: 0,
-        })
+            device: None,
+            device_error: None,
+        };
+        prover.try_open_device();
+        Ok(prover)
     }
+}
 
+impl<E: Field + Ring + Unreduced> RelationRangeImageProver<E> {
     /// Return the fully folded witness evaluation after the final round.
     ///
     /// # Panics
diff --git a/crates/akita-prover/src/protocol/sumcheck/relation_range_image/mod.rs b/crates/akita-prover/src/protocol/sumcheck/relation_range_image/mod.rs
index fc38730..fc709be 100644
--- a/crates/akita-prover/src/protocol/sumcheck/relation_range_image/mod.rs
+++ b/crates/akita-prover/src/protocol/sumcheck/relation_range_image/mod.rs
@@ -270,6 +270,9 @@ pub struct RelationRangeImageProver<E: Field> {
     scan_time_total: f64,
     fold_time_total: f64,
     rounds_completed: usize,
+
+    device: Option<device::DeviceRounds<E>>,
+    device_error: Option<AkitaError>,
 }
 
 mod additional_terms;
@@ -278,6 +281,8 @@ mod coefficient_prefix;
 mod coefficient_round_fold;
 mod compact_prefix;
 mod dense_terms;
+pub mod device;
+mod device_flow;
 mod evaluation_trace;
 mod lane_prefix;
 mod lifecycle;
diff --git a/crates/akita-prover/src/protocol/sumcheck/relation_range_image/round_flow.rs b/crates/akita-prover/src/protocol/sumcheck/relation_range_image/round_flow.rs
index 6b485a7..4c203af 100644
--- a/crates/akita-prover/src/protocol/sumcheck/relation_range_image/round_flow.rs
+++ b/crates/akita-prover/src/protocol/sumcheck/relation_range_image/round_flow.rs
@@ -1,6 +1,8 @@
+use super::device_flow::DeviceFold;
 use super::*;
+use akita_serialization::AkitaSerialize;
 
-impl<E: Field + Ring + Unreduced> RelationRangeImageProver<E> {
+impl<E: Field + Ring + Unreduced + AkitaSerialize> RelationRangeImageProver<E> {
     fn finish_ingested_round(&mut self, fold_started: Instant) {
         self.rounds_completed += 1;
         if self.rounds_completed < self.num_vars {
@@ -23,25 +25,34 @@ impl<E: Field + Ring + Unreduced> RelationRangeImageProver<E> {
 
     pub(super) fn compute_current_round_poly_from_state(&mut self) -> UniPoly<E> {
         let t_scan = Instant::now();
+        let device_terms = self.device_round_terms();
+        let parity = self.device.as_ref().is_some_and(|d| d.parity);
         let (poly, norm_poly) = match &self.relation_state {
-            RelationRoundState::ReducedDense { weights: dense } => match &self.witness_state {
-                WitnessState::CompactPrefix(compact_witness) => {
-                    let (virt_terms, rel_terms) = self.compute_round_compact_reduced_dense_terms(
-                        compact_witness.view(),
-                        dense.evaluations(),
-                    );
-                    let (norm_poly, relation_poly) = self.polys_from_terms(virt_terms, rel_terms);
-                    (self.combine_polys(&norm_poly, &relation_poly), norm_poly)
-                }
-                WitnessState::FoldedSuffix(folded_witness) => {
-                    let (virt_terms, rel_terms) = self.compute_folded_reduced_dense_round_terms(
-                        folded_witness,
-                        dense.evaluations(),
-                    );
-                    let (norm_poly, relation_poly) = self.polys_from_terms(virt_terms, rel_terms);
-                    (self.combine_polys(&norm_poly, &relation_poly), norm_poly)
-                }
-            },
+            RelationRoundState::ReducedDense { weights: dense } => {
+                let cpu_terms = || match &self.witness_state {
+                    WitnessState::CompactPrefix(compact_witness) => self
+                        .compute_round_compact_reduced_dense_terms(
+                            compact_witness.view(),
+                            dense.evaluations(),
+                        ),
+                    WitnessState::FoldedSuffix(folded_witness) => self
+                        .compute_folded_reduced_dense_round_terms(
+                            folded_witness,
+                            dense.evaluations(),
+                        ),
+                };
+                let (virt_terms, rel_terms) = match device_terms {
+                    Some(dev) if !parity => dev,
+                    Some(dev) => {
+                        let cpu = cpu_terms();
+                        self.check_device_terms(&cpu, &dev);
+                        cpu
+                    }
+                    None => cpu_terms(),
+                };
+                let (norm_poly, relation_poly) = self.polys_from_terms(virt_terms, rel_terms);
+                (self.combine_polys(&norm_poly, &relation_poly), norm_poly)
+            }
             RelationRoundState::QuotientFactored { weights, .. } => {
                 self.compute_quotient_round_from_state(weights)
             }
@@ -50,7 +61,9 @@ impl<E: Field + Ring + Unreduced> RelationRangeImageProver<E> {
         self.scan_time_total += t_scan.elapsed().as_secs_f64();
         poly
     }
+}
 
+impl<E: Field + Ring + Unreduced> RelationRangeImageProver<E> {
     fn compute_quotient_round_from_state(
         &self,
         weights: &RelationWeightFactorization<E>,
@@ -162,6 +175,15 @@ impl<E: Field + Ring + Unreduced + Fold> RelationRangeImageProver<E> {
     fn ingest_reduced_dense_challenge(&mut self, r: E) {
         self.split_eq.bind(r);
         let folding_lane_round = !self.in_coefficient_round();
+        let parity_tables = match self.device_ingest(r) {
+            DeviceFold::Skip => {
+                if folding_lane_round {
+                    self.live_lane_count = self.live_lane_count.div_ceil(2);
+                }
+                return;
+            }
+            DeviceFold::Cpu(tables) => tables,
+        };
         self.witness_state = match mem::replace(
             &mut self.witness_state,
             WitnessState::FoldedSuffix(Vec::new()),
@@ -187,10 +209,15 @@ impl<E: Field + Ring + Unreduced + Fold> RelationRangeImageProver<E> {
         if folding_lane_round {
             self.live_lane_count = self.live_lane_count.div_ceil(2);
         }
+        if let Some((witness, weights)) = parity_tables {
+            self.check_device_tables(&witness, &weights);
+        }
     }
 }
 
-impl<E: Field + Ring + Unreduced + Fold> SumcheckInstanceProver<E> for RelationRangeImageProver<E> {
+impl<E: Field + Ring + Unreduced + Fold + AkitaSerialize> SumcheckInstanceProver<E>
+    for RelationRangeImageProver<E>
+{
     fn num_rounds(&self) -> usize {
         self.num_vars
     }
