# W5 Wave-2 Implementation Tickets (W5-spec, 2026-07-31)

Read-only spec lane. Sources: journal (W4-M canonical + traced snapshot, W3-T4 attribution, W4-R engine notes, W4-D fix), `.webgpu-w3-specs.md` (format + standing kernel rules), `~/dev/jolt/.metal-m5-cuda-port-audit.md` (X-ticket map), wave1 tree @7912a9194 (read), upstream lineage measured with git directly.

## 0. Fact base

**Wave-1 state:** jolt `webgpu/wave1` @7912a9194 (worktree `~/dev/jolt/.worktrees/w4-m`), demo `webgpu/wave1` @b9d18e1 (`.worktrees/w4-m-verify`, includes UI + W4-D fix). Canonical @2^22: **ON 36.41s / OFF 80.53s** (2.43× frozen 88.44, 2.21× same-tree); @2^20 ON 13.70 / OFF 31.09. **Canonicals are jacobian-default — W4-D flipped `bucket_xyzz` default ON after them ⇒ next re-anchor is xyzz-default and supersedes.** Soak 10/10 @2^22 zero wedges; cross-GPU integrity guards live (commit + combine_hints readbacks on-curve-checked, CPU decline).

**Traced ON composition @2^22** (shares of ~38s traced; ×36.41 for untraced-scale): st0 41.8% ≈ 15.2s (miller absorb pool ~8–10s + i128 feeds ~0.9–2.4s overlapped + cache prime ~2.2s run-1-only + commit device ~1s post-CSR + extract ~0.5s), st1 10.3% ≈ 3.7s, st6b 10.3% ≈ 3.7s (BytecodeReadRafCycle 1.53 + covered residue), st8 10.3% ≈ 3.7s, st5 6.9% ≈ 2.5s, remainder 20.4% ≈ 7.3s = st2+st3+st4+st6a+st7.

**Rates (unchanged):** GPU Tint 0.865–0.93 Gmul/s composed/packed; CPU-WASM 0.149 all-core Fr; fold advantage 2.7–6.2×; browser round floor 0.74ms + 3.4ns/elem (T2a step0); readback-in-gpu-worker ≈ 1ms class, NOT 5–15ms. Slots live: commit(CSR), inc, InstrRaVirt, BooleanityCycle, RamRaVirt, InstructionReadRaf, JointOpening, miller hook, dory hooks.

**Standing rules (all tickets, from W3 spec §0 — still binding):** ≤2 add-class EC inlines/loop body; tiny-first probe every new kernel shape; tables in storage/uniform never module-scope const; Fe8 packing on streamed operands; 65535-wg audit + ≥2^22 test leg per new per-element kernel; timed passes under lock + ioreg gate + identity protocol (unique port, served-hash==pkg, PID kills); perf truth = Tint only. **New since W3:** (a) engine or slot changes ⇒ 10-run @2^22 re-soak (watchdog channel live); (b) **timed CPU-arm runs must goto `/?webgpu=0` or `/bench.html`** — the built app now warms the GPU arm on `/` (W4-UI); (c) correctness on OTHER dies is not implied by M4 gates — e2e validity gate on the target machine is the only sufficient check (W4-D).

---

## U0 — upstream rebase DECISION: **GO, MERGE FIRST, tonight** (it's a merge, not a rebase re-derivation)

**Measured lineage:** `git merge-base 70a294ad5 be900fc55` = **70a294ad5** — the new upstream head is a **strict descendant** of wave1's base (36 commits, +8186/−925 over 149 files; no rebase upstream — they merged main in). So this is `git merge be900fc55` into `webgpu/wave1`, with conflicts only where both sides touched files. **The feared T2a-style seam re-derivation does not exist.**

**Diff exposure on our seamed surface (git diff --stat 70a294ad5..be900fc55):** `optimized/` = **+420/−100 over 17 files**. Per file: backend.rs +17 (Allocative impl only — zero overlap with our webgpu() ctor), lazy_ra.rs ±10, instruction_read_raf.rs ±12, booleanity.rs ±19, bytecode_read_raf.rs ±53, ram_ra_virtualization.rs ±77, **commitment.rs ±77** (the zk×optimized bounds fix W-PR2 wrote and upstream folded: `+ ModeStreamingCommitment`, `finish_with_hint`→`finish_streamed` — our webgpu/commitment.rs consumer takes the same bounds, mechanical), **registers_read_write.rs ±148** (CoeffLut u64 fix NOT upstreamed — patch persists, expect the one real conflict), testing.rs ±54. **jolt-dory: ZERO drift** (W3a cache fix, T3 hooks, T1's prepared_srs_g2_range merge clean). jolt-field: zero. **dory-pcs stays 0.4.0 ⇒ T1's vendor [replace] survives verbatim.** The 36 commits are: BlindFold #1690 merge, zk×optimized fix, per-stage perfetto/telemetry (stages/*.rs +1 line each; new jolt-profiling machinery + 382-line modular_benchmark example), dep bumps (sha3 0.12, serde, log), CI.

**What it buys:** (1) **PR-ability** — origin main (jolt.rs) runs the modular CPU prover at this lineage (PR#3 merged, PR#4 pin = be900fc55); the webgpu arm's eventual upstream PR needs this base. (2) **zk×GPU** — BlindFold on-tree with a zk e2e on `JoltBackend::optimized()`; our `webgpu()` extends `optimized()` ⇒ zk×GPU becomes a testable config next wave. (3) Upstream telemetry/profiling (useful, may retire some of our tooling). (4) CoeffLut patch status: still ours (ship in the eventual PR).

**What it costs:** ~1–1.5 day-class: merge + ~6 conflict files (+420/−100 total drift) + full gate battery (naga/dory/Tint-26/native bytes ×2 guests/browser @2^16) + **the re-anchor we owe anyway** (xyzz-default canonicals). W-PR2 measured old↔new pin ≈ **WASM perf parity** (sha2 1.82 new vs 2.09 old; @2^20 parity) ⇒ no perf risk. Byte-stability gate: native hashes vs 127b0545…/f4bbda7c… — if they move, diagnose (sha3 bump is guest-side, zk refactor is non-zk-identical by design) before accepting; re-pin gate hashes with the reason documented.

**ORDER arithmetic (before vs after wave-2 lanes):** merge now = 1–1.5d, conflicts on 6 files. Merge after = the same 6 files PLUS U2's ~6–8 new seams in `optimized/` stage kernels + U4a's bytecode_read_raf.rs seam all built on a dead base, re-validated post-merge, and a second full re-anchor (bench windows are the scarce resource — ~2–3h each on this shared box). Double-pay ≈ +1–1.5d + one extra gate battery + anchor churn mid-wave. **Rebase first. Only U5 (fully independent) and U1's build phase (webgpu/-owned files, zero upstream overlap) may start pre-merge.**

**Scope:** jolt: merge be900fc55 → wave1, resolve (commitment.rs bounds, registers_read_write.rs CoeffLut, small files), grep-check match-with-catch-all fns on every shared-file merge (W4-M polarity lesson). Demo: merge origin/main (PR#3 merge @7d09b61 lineage) → demo wave1 (worker.js/Cargo pins/patches — small). Gates: full battery + engagement checks + 65535 re-audit unchanged shapes. Re-anchor: OFF/ON @2^20+2^22 med5 xyzz-default + jac A/B singles (feeds U4c), dashboard point, soak 10 @2^22.
**Acceptance:** all gates green; bytes stable or delta explained; ON @2^22 within ±3% of 36.41 (xyzz shift noted separately); new canonical blessed. **Effort: 1–1.5d.** Kill: none (descendant merge can always be completed; if a gate goes red, bisect the 36 upstream commits — they're small).

---

## U1 — miller absorb residue (largest remaining pool)

**Pool:** st0 traced ON 15.9s @2^22; the attackable slice = **miller absorb pool ~8–10s wall** (T4 attribution: 79.4 worker-s waits; W4-M: st0-minus-commit-minus-feeds). Physical floor ≈ **3–4s**: 345K pairs × ~4.2K Fq-mul ≈ 1.45 Gmul ÷ 0.865 ≈ 1.7s device ALU + ~1.1s traffic (42 absorbs × 2.4GB @90GB/s) + CPU shard + GT/readbacks.

**What remains after T1+W4-R (why the pool survives):** (a) **occupancy**: 8192 pairs/call = 8192 threads ≈ 0.75–0.83 of roof (thread ladder), ~300 serial dispatches/call with inter-dispatch bubbles; (b) **call serialization**: 42 commit absorbs + 26 st8 multipairs arrive concurrently; W4-R detached the READBACKS (encode jobs now µs–ms) but each ~300-dispatch pass still owns the device serially, and the span-level overlap win was deferred to the merge re-anchor and never measured; (c) **CPU shard f=0.1** ≈ 34.5K pairs ≈ 23 CPU-s ≈ 2.3s wall — tuned pre-CSR/pre-detached/pre-composition; (d) GT tree-reduce + 384B readback per call.

**Attack (measure-first — the pool has never been decomposed on the merged tree):**
1. **Step 1 (0.5d): traced attribution** — split the absorb pool into device-busy / queue-wait / CPU-shard / GT+readback (spans exist: miller_webgpu, engine introspect pending_readbacks gauge). Pick attacks by the split.
2. **Absorb coalescing** (if queue-wait/bubbles dominate — expected): merge queued absorb jobs into one mega-Miller pass at the engine or hook layer — 2–4 calls × 8192 pairs ⇒ 16–32K threads (0.83–0.88 saturation) + amortized dispatch bubbles + one GT readback per batch. GT results return per-call ⇒ transcript order = caller consumption order, bytes identical by construction (partition invariance already gated).
3. **2-pairs-per-thread A/B** (Metal MILLER_SEG_PAIRS=2 won 6.6 vs 12.2 µs/pair @8) — one-knob kernel-geometry experiment, cheap.
4. **Fraction re-sweep** {0, 0.05, 0.1, 0.2} quiet-window on the final composition (0.1 was blessed in a loaded era, pre-everything).
5. **Subgroup 6-lane coop Miller** (CUDA X31, regs 239→128) — ONLY if 1–4 leave the pool >6s; new kernel family, expensive, park by default.

**Files:** `webgpu/miller.rs`, `webgpu/engine.rs` (queue/coalesce — append-only API per W4-R note), `mod.rs` options. All campaign-owned; zero upstream collision ⇒ build phase may start pre-U0, rebase trivially.
**Gates:** existing miller byte gates (off/f=0/f=0.1/f=1 SHA-identical, Tint 15/15 schedules, partition invariance) + coalesced-batch partition KAT; **engine changed ⇒ 10-run @2^22 re-soak**; naga fq6 quarantine stays (Tint/wasm-only serve).
**Acceptance:** st0 traced ON ≤ **10s @2^22** (from 15.9); whole-prove ≥ **−3.5s**; @2^20 no regression (absorbs are 2048-pair there — coalescing helps MORE); bytes identical; soak clean. Kill: attribution shows ≥70% of pool is device-busy ALU at saturated occupancy ⇒ pool is physical, bank steps 3–4 only, close at −2s.
**Effort: 2–2.5d.** Mid **−4.0s**, high **−6.0s** @2^22.

---

## U2 — serial stages (st1–4, st6a, st7 ≈ 11s ON @2^22)

**Classification (per W2c economics + W1d rates):** every stage here is Fr-sumcheck/fold-shaped ALU work — the 2.7–6.2× GPU surface (WASM tax is ALL ALU, 3.9×) — NOT bandwidth-parity streaming. T2a proved the class in-browser at **4.9×** on covered consumers; step0 killed the round-latency fear (0.74ms floor). CUDA precedent: s1–s3 pool 7.13→0.64 @2^25 (−91%). Split into two tickets by risk:

### U2b — round-ladder family (proven template; dispatch first)

**Pool ≈ 6.2s ON:** st3 InstructionInput ~1.6s (rounds = 77% of stage; donors: Metal W9 slot ✅ + CUDA ✅), st4 RegistersRWC ~1.5s (CUDA ✅; **CoeffLut file — post-U0 only**) + RamValCheck (CUDA ✅) + registers_claim_reduction (X24-F, low), st2 RamRWC ~1.0s (**CUDA X24-G: device sparse cycle rounds + early device→host handoff at ≤4096 rows — parity BY CONSTRUCTION, port as-is**) + product/claim residue, st7 HammingWeightCR ~0.7s (Metal W2 slot ✅), st5-residue RegistersValEvaluation (low). All ride the inc/T2a template verbatim: device ping-pong, fused fold+eval, lane-major partials + lane_reduce, ≤320B/round readback, early host handoff (default 2^12), decline-at-admission fail-closed.

**Files:** new `webgpu/slots/{instruction_input, registers_read_write, ram_read_write, hamming_weight_cr, ...}.rs`; seams in `optimized/{instruction_input, registers_read_write, ram_read_write, ram_val_check, hamming_weight_claim_reduction, registers_claim_reduction}.rs` (factory-closure prepare fronts, T2a pattern — re-derive against post-U0 trunk); shared appends wgsl.rs/context.rs/mod.rs. Watch pipeline count (warmup was 2695ms @41 kernels — budget +6–10).
**Gates:** per-slot naga KATs + lockstep vs optimized + Tint vectors + native bytes forced × handoff {64, default, ∞} + browser @2^16 + ≥2^22 leg + **@2^20 sign check per slot** (T2b's net-negative precedent: diagnose shape before gating; per-slot min_terms floor only as last resort). Each slot extends the forced-env byte list (CUDA's 14-env discipline).
**Acceptance:** st2+st3+st4+st7 ≤ **3.5s ON @2^22** (from ~6.5–7 incl. claim-reduction residue); whole ≥ −3.5s; @2^20 whole non-negative per slot. Kill: first two slots net-negative @2^22 after shape diagnosis ⇒ stop the front (W5 criterion).
**Effort: 2.5–3d.** Mid **−4.2s**, high **−5.0s**.

### U2a — spartan front st1 (X24-B; the L-class item — after U2b proves the wave still pays)

**Pool ≈ 3.7s ON** (prepare 47% / OuterRemainder 30% / batch prove 15%). CUDA recipe: uniskip t1 one-launch (`jolt_x24_outer_t1`, fr_kernels.cu:10276), az/bz device materialization from the trace sideband, per-round Gruen endpoints device-reduced ≤2 Fr/round, output claims one launch (cuda:spartan_outer.rs:1–31). **Cost drivers:** the i192/i256 integer Lagrange kernels need WGSL u32-limb twins (new kernel family, KAT-heavy); the packed-rows 12-word layout may not carry the spartan sideband fields — extend the P2 layout (generation-bump) or add a second resident upload. Phase 1 = rounds + OuterRemainder on the inc template (az/bz stays host): mid −1.2s. Phase 2 = az/bz device materialization: high −2.2s. Files: `webgpu/slots/spartan_outer.rs` (new), `optimized/{spartan_outer, spartan_product, spartan_shift}.rs` seams, packed_rows.rs layout rev.
**Acceptance:** st1 ≤ **2.4s ON @2^22** (from 3.7); bytes identical (integer-twin KATs exact — the CUDA parity recipe is host `gruen_poly_deg_3` assembly + handoff-verbatim). Kill: Phase-1 lands <−0.6s ⇒ skip Phase 2. **Effort: 3–4d.** Mid **−1.2s**, high **−2.2s**.

### Honest CPU remainder (not ticketed)

st6a 1.1s (X30 K-bin sort + segmented reduce — S-M port cost for ~0.7s: descope), st1 prepare table-walks if az/bz stays host (~1–1.5s), Stage-batch glue + transcript (~0.5s). ≈ **2.5–3s of the 11s pool stays CPU this wave.**

---

## U3 — 2^23 scale attempt (probe ticket, capability headline)

**Facts:** CPU arm @2^23 = OOM at the 4GiB cap (W1a). GPU arm @2^23 **never attempted**. ON-arm heap @2^22 ≈ 3.19–3.25GB single-run plateau (4.08GB session ratchet); T-linear host state doubles ⇒ projected ~5.5–6.5GB ⇒ OOM unless ~2GB shed. Device buffers live OUTSIDE the wasm heap (W1b: 18GiB held in-browser) — residency is the lever. 65535 audit verdict: kernel shapes safe through 2^23, first breach @2^24 (U4e hardens). **The tree offers NO small-space/streaming prover hooks** (grep-verified: no small_space/StreamingProver); the witness is already superchunk-streamed — the retained heap is tables + trace + staging, i.e. OUR shed list, not an upstream flag.

**Probe (1d):**
1. Run GPU-arm @2^23 (sha2-chain 2223 iters; SRS already sized to 2^23 max_trace_length) with W4-R telemetry + a wasm allocator watermark hook; capture the failing allocation site + heap census at 3 checkpoints (post-trace, mid-st0, st5).
2. Write the **shed ledger**: top-5 host allocations × device-residency/streaming feasibility. Expected entries: inc-table source slices (4×T×32B = 1GB @2^23 — stream per-chunk into device staging as generated, drop the host Vec), packed-rows host copy (~400MB — uploaded once; droppable if CPU fallback declines instead of demoting), LazyRa host Pending retention, one-hot superchunk staging (2×2^22×80B ≈ 672MB pipelined — superchunk sizing knob), trace Vec<Cycle> (measure size_of::<Cycle>()×8.4M — likely the irreducible core), st5 u_evals host copy timing.
3. Verdict: ≤3 targeted sheds project under ~3.8GB ⇒ **GO for U3b** (1–2d implementation: shed + prove + validate + 65535 legs @2^23 re-run live). Else **park with the ledger** + a written statement of what real streaming would need (that becomes an upstream conversation, not a lane).

**Bounded-config honesty:** ram_K/memory-config reduction shrinks K-side tables only; T-side dominates @2^23 — the config lever is small, say so in the report rather than shipping a gerrymandered "2^23".
**Acceptance (U3b):** one VALID in-browser 2^23 proof on the GPU arm, any wall (capability, not speed — CPU arm cannot do this at all); heap ledger published; no silent-corruption legs (every slot's ≥2^22 test leg re-run at 2^23 where geometry allows). Kill: census shows >3.5GB irreducible (trace+prover state alone) ⇒ park, publish ledger. **Effort: 1d probe (+1–2d if GO).** Bench-lock note: 2^23 proves are ~80–120s-class runs — schedule in the lock queue like any ladder.

---

## U4 — quick-wins bundle (one lane, itemized)

| item | what | pointer | est |
|---|---|---|---|
| **a. BytecodeReadRafCycle twin** | inc-style twin owning a device `combined` ping-pong from round 0 + the ra gather family; host fallback via `from_bound_state` ctor | design written: T2a §follow-ups; CUDA X19b precedent 681→29ms; files: `webgpu/slots/ra_lazy.rs` + `optimized/bytecode_read_raf.rs` (+53 upstream drift ⇒ post-U0) | **−1.0…1.2s** @2^22 (pool 1.53s) |
| **b. per-superchunk guard patch-up** | on off-curve guard trip, CPU-redo ONLY the tripped superchunk's columns instead of poisoning the whole commit pass (fallback plumbing exists; commitments are per-column) | W4-D wave-2 item; `webgpu/commitment.rs` | M5-class dies: guard-trip cost ~+3s → ~+1.5s; M4: 0 |
| **c. XYZZ verdict** | paired quiet-window A/B jac↔xyzz @2^20+2^22 ×3 medians; keep-or-revert the W4-D default; document | T4 singles were contradictory; **rides U0's re-anchor windows — cheap** | 0…−1s (unknown; also integrity-relevant on M5) |
| **d. fold/G2 readback guards** | extend `jac_on_curve` to joint_opening fold readbacks + G2 variant (y²=x³+3/ξ over Fq2) + dory_folds/hint paths; fail-closed decline mirrors commit pattern | W4-D follow-up ("currently fail-loud-at-verify, not fail-closed"); `webgpu/points.rs`, `dory_folds.rs`, `slots/joint_opening.rs` | ~0 perf (5 muls/readback-pt); closes the M5 exposure class |
| **e. 2^24 dispatch hardening** | inc/ra 1D round sites: debug_assert → release-visible cap check + fail-closed decline | W4-M audit ("first breach = inc/ra 1D rounds at 2^24, debug_assert-only in release") | insurance for U3-era scales |

**Gates:** (a) full slot battery incl. @2^20 sign check; (b) synthetic guard-trip test (forced bad point) shows column-local fallback + valid proof; (d) KATs + native/browser bytes; (e) release-build cap test. **Acceptance:** st6b −1.0s+; verdict (c) documented + canonical updated; guards green. **Effort: 1.5–2d.** Collision: (a) shares `ra_lazy.rs` — do NOT run concurrently with U2b if U2b's claim-reduction work touches the same file (it shouldn't; check at dispatch).

---

## U5 — upstream corruption-repro artifact (user-visible; independent)

**Goal:** minimal standalone repro of the W4-D M5×Chromium-145 commit-bucket corruption for a Chromium/Apple bug report. **No wasm, no proof stack**: single HTML+JS+WGSL page — deterministic synthetic CSR bucket workload (the corrupting shape: `bucket_madd_csr`(+xyzz) at production occupancy, 40K+ threads @2^18-equivalent and 164K @2^20-equivalent), N repeat dispatches, JS BigInt on-curve check (y²=x³+3) + cross-run determinism diff. Expected matrix: M4 = clean at all occupancies (negative control); M5×Cr145 = off-curve/nondeterministic at production occupancy, clean at KAT scale (~10² threads — exactly why KATs never saw it). WGSL source: export from the vectors dump (`crates/jolt-kernels/target/wgsl-vectors` — already contains the exact kernels).

**Steps:** (1) build page + local M4 negative-control run (0.5d); (2) validate corruption on macbook-home via remote hands (codex task; W4-D staging persists: `~/jolt-webgpu-debug`, Chromium 145 at `~/w4d-chrome145`); (3) **minimize** while corruption persists (bisect thread count / dispatch count / kernel pair — smaller repro = stronger report); (4) package README (die×browser matrix, how to read failures) → report draft for Chromium tracker + Apple Feedback; **user files it** (message_parent with artifact path, don't submit externally).
**Acceptance:** repro fires on M5 (either browser build that corrupts), clean on M4, ≤2 files, README complete. Kill: 3 sessions without an M5 repro outside the full engine ⇒ the corruption needs engine-scale state — document that negative honestly (it's still bug-report-relevant). **Effort: 0.5–1d local + one remote validation session.** Zero tree collision (new demo-side files only). NOTE: M5×Chrome-150 cell stays untested — no Chrome app on that box; user-level install, flag only.

---

## The arithmetic — wave-2 target ledger @2^22 (from ON 36.41s)

| step | pool | Δ mid | Δ high | wall after (mid) |
|---|---|---|---|---|
| U0 merge + re-anchor | — | 0 (gate = parity ±3%) | — | 36.4 |
| U1 miller residue | 8–10s | −4.0 | −6.0 | 32.4 |
| U4a twin (+b–e) | 1.5s | −1.1 | −1.4 | 31.3 |
| U2b round ladders | ~6.2s | −4.2 | −5.0 | 27.1 |
| U2a spartan st1 | ~3.7s | −1.2 | −2.2 | 25.9 |
| U4c xyzz verdict | ? | 0 | −1.0 | **25.9** |

**Mid ≈ 26s ≈ 3.4× frozen (88.44) / 3.1× same-tree (80.53). High ≈ 21s ≈ 4.3× frozen.**

**Honest ceiling of this ticket set:** remaining at mid = miller floor ~4–5s + i128 feeds ~1–2s + st8 residue 3.7 + st5 residue 2.5 + st6b residue ~2.5 + serial-stage CPU remainder ~3 + witness/prime/misc ~2 ⇒ **floor ≈ 20–21s @2^22 (~4.3× frozen)** — the high column IS the ceiling. Past that = wave-3 material: device witness/table derivation (az/bz everywhere + trace-store builders, CUDA P3/X30 class), i128 device commit (Metal W5b signed-digit design), st8/st5 second passes, or upstream CPU wins. What stays physical: tracer emulation, transcript, CPU shards, readback floors.

**2^20 story:** ON 13.70 now. Pools scale: miller ~3–3.5s (2048-pair calls — coalescing helps more), serial ~3.3s, twin ~0.4s. Mid ≈ **9–9.5s ≈ 3.3× frozen 30.72**; high ≈ 8s. Every U2 slot carries the @2^20 sign check (T2b precedent).

---

## Collision map + lane plan

| surface | U0 | U1 | U2b | U2a | U3 | U4 | U5 |
|---|---|---|---|---|---|---|---|
| whole-tree merge | **owns** | — | — | — | — | — | — |
| `webgpu/miller.rs` + `engine.rs` | merge | **owns** | — | — | — | — | — |
| new `webgpu/slots/*` | — | — | **owns (per-stage)** | **owns spartan** | — | ra_lazy.rs (a) | — |
| `optimized/` stage seams | conflicts (6 files) | — | st2/3/4/7 files | spartan_*.rs | — | bytecode_read_raf.rs | — |
| `webgpu/commitment.rs` | zk-bounds conflict | — | — | — | reads | **owns (b)** | — |
| `packed_rows.rs` layout | — | — | — | rev (sideband) | reads | — | — |
| `wgsl.rs`/`context.rs`/`mod.rs` | merge | append | append | append | — | append | reads export |
| demo repo | main-merge | knobs | knobs | knobs | probe harness | — | **owns new page** |

**Dispatch order:** **tonight = U0 + U5 (+U1 build-phase)** — U5 fully independent; U1's files are campaign-owned (zero upstream overlap) but its benches queue behind U0's re-anchor. **Then U1(timed) ∥ U4 ∥ U5-remote-validation → U2b → U2a ∥ U3-probe (→U3b if GO).** Merge order into wave2 trunk: U0 → U1 → U4 → U2b → U2a (→ U3b). Two re-anchor points only: post-U0 (xyzz-default canonical, doubles as U0 gate + U4c A/B) and wave-close (canonical ladder + dashboard + user report).

**Bench-lock pressure:** one box, three campaigns — the lock is the serializer, not CPU. Budget: U0 re-anchor ~2–3h, U1 attribution+final ~2h, U2b per-slot sign checks + final ~2–3h, U3 probe runs ~1h, U4 windows ride U0/U1's. Standing: ≤30min holds, unique ports, served-hash assert, PID-only kills, ioreg gate, quiet windows, **`/?webgpu=0` or `/bench.html` for CPU-arm walls** (the `/` page now warms the GPU arm). Engine-touching lanes (U1, U2 slots) re-soak 10 @2^22 before merging.

**Remote hands (macbook-home):** required for U5 validation (the corrupting die; codex task, staging persists). Optional: U4b/d M5 verification (guard-trip cost halving is M5-visible). Blocked-on-user: M5×Chrome-150 cell (no Chrome app installed there).

**Effort totals:** U0 1–1.5d, U1 2–2.5d, U2b 2.5–3d, U2a 3–4d, U3 1d(+1–2), U4 1.5–2d, U5 0.5–1d ⇒ ~12–15 worker-days ≈ 4–5 calendar days at 2–3 parallel lanes.
